\section{Background and Motivation}\label{sec:intro:background}

Recent large-scale conflicts illustrate how contested the electromagnetic spectrum has become.
Commanders rely on timely, passive sensing to build situational awareness, protect platforms, and coordinate fires without revealing their own emissions.
Within \gls{ew}, which aims to exploit, protect, and maneuver in the spectrum, \gls{esm} systems listen passively and supply the measurements that feed higher-level \gls{elint} tasks such as detecting radars, grouping pulses by emitter, and inferring behavior \parencite{wiley2006elint,skolnik2008radar}.

Modern radars are increasingly agile: they vary carrier frequency, pulse width, amplitude, and \gls{pri} across bursts to improve performance and survivability.
That agility blurs simple fingerprints and stresses classical rule-based parsers tuned to stable pulse trains.
The same physical emitter also switches \emph{modes}---recurrent control policies that map an operational objective (for example, volume search versus track) to a characteristic regime in the pulse train.
Mode awareness is therefore operationally informative: it constrains how the waveform may evolve, signals intent at a coarse level, and can help disambiguate emitters when type-level signatures overlap.

At the same time, \gls{ml} and \gls{dl} have become the default toolkit for sequence modeling and representation learning in other domains.
Radar pulse analysis naturally presents as a stream of low-level measurements---often summarized as \glspl{pdw}---where long-range dependence, mixture structure (multiple emitters superposed in time), and label scarcity all push toward learned encoders rather than purely hand-crafted feature pipelines.

This thesis studies whether a transformer-style encoder, trained with objectives suited to clustering, can produce embeddings in which both distinct emitters and their operating modes emerge as separable structure.
The remainder of the introduction states the problem precisely, lists the research questions, and outlines scope, contributions, and ethics.
