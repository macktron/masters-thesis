# Agent context — Chapter 5: Experiments and Results

## Purpose

Empirically answer the research questions stated in `sections/01_introduction/03_research_questions.tex`. Every table and figure should map back to one of those questions. Keep interpretation light here — saving discussion for Chapter 6.

## Target length

12–18 pages.

## Subsection contracts

- `01_setup.tex` — Hardware (GPU type, number), software stack (Python/PyTorch without pinning exact versions). Note that code is Saab-proprietary and not released. Do not mention training seeds.
- `02_datasets.tex` — Each dataset gets its own paragraph (or table row): description, size, number of emitters, number of modes, train/val/test split, synthetic vs. real, any preprocessing peculiarities.
- `03_hyperparameters.tex` — Optimisation schedule (`tab:exp:hparams_main`) plus per-model, per-branch DBSCAN $\varepsilon$ (`tab:exp:hparams_attn`). Rotary scales are $\gamma_t=\gamma_x=\gamma_y=10$ in the method chapter, not this table.
- `04_quantitative_results.tex` — Protocol plus RQ1 only. Joint Vanilla versus Feature DBSCAN, both branches in one table (`tab:exp:vanilla`). Do not introduce attention variants here. Protocol: all models scored at $L=2000$ with no overlap; neural dumps used stride $200$ and keep every tenth window ($960$ windows); Feature DBSCAN is already overlap $0$. Spread is over windows. Do not mention training seeds. Forward-ref the Vanilla panels of `hist_em.pdf` / `kcorr_em.pdf` in `06_attention_variants.tex`. Global mode catalogue $M=16$ is the max distinct mode count on those windows.
- `05_ablations.tex` — RQ2 only, before the attention bake-off. Vanilla backbone; Joint vs Emitter-only vs Mode-only; both branches decoded in every run. Joint numbers copied from `tab:exp:vanilla`, not a second run. Mode-only is part of the design (`final_vanilla_mode`). One table (`tab:exp:ablation_loss`). No extra distribution figures. Do not compare ablation counts to RoPE-TOA or Bias; that comparison belongs in `06_attention_variants.tex`.
- `06_attention_variants.tex` — RQ3. Joint attention variants: Vanilla, RoPE-TOA, RoPE-az, Bias, and RoPE-TOA+Bias (`final_combined`: TOA RoPE on trunk and both branches, plus the pairwise bias, no incidence RoPE; same train overlap $1800$ as the other neural runs). Include all five in the score and efficiency tables. Do not repeat Feature DBSCAN in the score tables; it stays in `tab:exp:vanilla` and in the efficiency table (`final_baseline`: no training, test $L=2000$, overlap $0`). Vanilla remains the baseline row. Tables: emitter (`tab:exp:attn_em`), mode (`tab:exp:attn_md`), parameters and encoder/clustering time (`tab:exp:efficiency`). Overlaid ARI ECDFs on a log vertical axis (`hist_{em,md}.pdf`) and count heatmaps (`kcorr_{em,md}.pdf`, 3×2 with one empty cell), generated by `scripts/plot_eval_figures.py`. Do not put Feature DBSCAN in those figures. A factual link to the ablation is in scope: Emitter-only Vanilla already reaches the closed emitter range; RoPE-TOA and Bias reach it under the joint objective; RoPE-TOA+Bias does not.
- `07_qualitative_analysis.tex` — Feature plots once (window 100, emitter vs mode coloring, plus one crowded mode window). UMAP embeddings of window 100 for all five joint models, both branches. Drop-ins in `figures/experiments/` (`feat_w100_{em,md}.png`, `feat_maxmode.png`, `emb_*_w100_{em,md}.png` including `emb_combined_w100_{em,md}.png`).

## Style notes

- Tables: use `booktabs` (`\toprule`, `\midrule`, `\bottomrule`) and `siunitx` `S` columns for numerical alignment once numbers exist. Bold winners with `\textbf{...}`.
- Numbers: report 3 significant figures. Table spread is over windows. Do not mention training seeds.
- Figures live in `figures/experiments/`.
- Never report a number without saying which metric, which dataset, and how it was aggregated.
- Avoid claims like "significantly better" without a stated test. "Higher by X std" is acceptable.
