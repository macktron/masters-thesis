# Agent context — Chapter 5: Experiments and Results

## Purpose

Empirically answer the research questions stated in `sections/01_introduction/03_research_questions.tex`. Every table and figure should map back to one of those questions. Keep interpretation light here — saving discussion for Chapter 6.

## Target length

12–18 pages.

## Subsection contracts

- `01_setup.tex` — Hardware (GPU type, number), software stack (Python/PyTorch without pinning exact versions). Note that code is Saab-proprietary and not released. Do not mention training seeds.
- `02_datasets.tex` — Each dataset gets its own paragraph (or table row): description, size, number of emitters, number of modes, train/val/test split, synthetic vs. real, any preprocessing peculiarities.
- `03_hyperparameters.tex` — Optimisation schedule (`tab:exp:hparams_main`) plus per-model, per-branch DBSCAN $\varepsilon$ (`tab:exp:hparams_attn`). Rotary scales are $\gamma_t=\gamma_x=\gamma_y=10$ in the method chapter, not this table.
- `04_evaluation_protocol.tex` — Scoring protocol for the rest of the chapter. All models scored at $L=2000$ with no overlap; neural test dumps are native overlap $0$ ($1168$ windows over $244$ scenarios); Feature DBSCAN is already overlap $0$ ($1412$ windows). Spread is over windows. Do not mention training seeds. Roadmap: RQ1 in `05_vanilla_vs_dbscan.tex`, RQ2 in `06_joint_objective.tex`, RQ3 in `07_attention_variants.tex`, qualitative in `08_qualitative_analysis.tex`. Labels: `sec:exp:protocol` and `sec:exp:results:protocol`.
- `05_vanilla_vs_dbscan.tex` — RQ1 only. Joint Vanilla versus Feature DBSCAN, both branches in one table (`tab:exp:vanilla`). Do not introduce attention variants here. Labels: `sec:exp:results` and `sec:exp:results:vanilla`. Forward-ref the Vanilla panels of `hist_em.pdf` / `kcorr_em.pdf` in `07_attention_variants.tex`. Global mode catalogue $M=17$ is the max distinct mode count on the neural test windows.
- `06_joint_objective.tex` — RQ2 only, before the attention bake-off. Vanilla backbone; Joint vs Emitter-only vs Mode-only; score the active branch only (Joint scores both). Dual-branch architecture kept; inactive branch excluded from the backward pass and not scored. Joint numbers copied from `tab:exp:vanilla`, not a second run. Mode-only is part of the design (`final_vanilla_mode`). One table (`tab:exp:ablation_loss`). Count heatmaps `kcorr_ablation_{em,md}.pdf` (1×2: emitters Joint vs Emitter-only; modes Joint vs Mode-only), generated by `scripts/plot_eval_figures.py` from `agg_results_new/`. Joint panels are the Vanilla evaluation, not a second run. Vanilla joint-vs-separate UMAP (`emb_vanilla_joint_vs_separate.pdf`, windows 200 and 500, Joint vs active single-task) is in this section; do not add ECDFs. Do not add RoPE-TOA or Bias single-task tables. Do not compare ablation counts to RoPE-TOA or Bias; that comparison belongs in `07_attention_variants.tex`. Do not report unused-head scores. Label: `sec:exp:ablation`.
- `07_attention_variants.tex` — RQ3. Joint attention variants: Vanilla, RoPE-TOA, RoPE-az, Bias, and RoPE-TOA+Bias (`final_combined`: TOA RoPE on trunk and both branches, plus the pairwise bias, no incidence RoPE; same train overlap $1800$ as the other neural runs). Include all five in the score and efficiency tables. Do not repeat Feature DBSCAN in the score tables; it stays in `tab:exp:vanilla` and in the efficiency table (`final_baseline`: no training, test $L=2000$, overlap $0$, $1412$ windows). Vanilla remains the baseline row. Tables: emitter (`tab:exp:attn_em`), mode (`tab:exp:attn_md`), parameters and encoder/clustering time (`tab:exp:efficiency`). Overlaid ARI ECDFs on a log vertical axis (`hist_{em,md}.pdf`) and count heatmaps (`kcorr_{em,md}.pdf`, 3×2 with one empty cell), generated by `scripts/plot_eval_figures.py` from `agg_results_new/` (no stride-10 subsample). Do not put Feature DBSCAN in those figures. Count-heatmap reading matches `kcorr_ablation_{em,md}.pdf` in `06_joint_objective.tex`. A factual link to the objective comparison is in scope: Emitter-only Vanilla already reaches the closed emitter range; RoPE-TOA and Bias reach it under the joint objective; RoPE-TOA+Bias does not. One sentence may note that an untabulated emitter-only RoPE-TOA run matches joint RoPE-TOA on emitters. Do not tabulate Bias or RoPE-TOA single-task runs.
- `08_qualitative_analysis.tex` — Feature plots once (test scenario 1752, evaluation index 100 on the original overlapping dump, emitter vs mode coloring, plus one crowded mode window). UMAP embeddings of that same window for all five joint models, both branches. Do not swap those panels to window 100 of `agg_results_new`. Drop-ins in `figures/experiments/` (`feat_w100_{em,md}.png`, `feat_maxmode.png`, `emb_*_w100_{em,md}.png` including `emb_combined_w100_{em,md}.png`). Single-task UMAPs stay in `06_joint_objective.tex`.

## Style notes

- Tables: use `booktabs` (`\toprule`, `\midrule`, `\bottomrule`) and `siunitx` `S` columns for numerical alignment once numbers exist. Bold winners with `\textbf{...}`.
- Numbers: report 3 significant figures. Table spread is over windows. Do not mention training seeds.
- Figures live in `figures/experiments/`.
- Never report a number without saying which metric, which dataset, and how it was aggregated.
- Avoid claims like "significantly better" without a stated test. "Higher by X std" is acceptable.
