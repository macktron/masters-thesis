# Agent context — Chapter 5: Experiments and Results

## Purpose

Empirically answer the research questions stated in `sections/01_introduction/03_research_questions.tex`. Every table and figure should map back to one of those questions. Keep interpretation light here — saving discussion for Chapter 6.

## Target length

12–18 pages.

## Subsection contracts

- `01_setup.tex` — Hardware (GPU type, number), software stack (Python/PyTorch without pinning exact versions). Note that code is Saab-proprietary and not released. Do not mention training seeds.
- `02_datasets.tex` — Each dataset gets its own paragraph (or table row): description, size, number of emitters, number of modes, train/val/test split, synthetic vs. real, any preprocessing peculiarities. Feature-over-ToA plots live here: scanning single mode (`feat_scan.png`, `fig:exp:feat_scan`), $3$/$1997$ emitter split (`feat_imbalance.png`, `fig:exp:feat_imbalance`), crowded emitters from `agg_results_new/final_vanilla/` (`feat_maxemitters.png`, `fig:exp:feat_maxemitters`, window 1083 ground-truth coloring), crowded modes (`feat_maxmode.png`, `fig:exp:feat_maxmode`). Do not add the five-model UMAP gallery; the joint-vs-separate UMAP lives in `sections/08_appendix/A_additional_results.tex` (`app:embeddings`), not in this section. Do not cite window indices in this section.
- `03_hyperparameters.tex` — Optimisation schedule (`tab:exp:hparams_main`) plus per-model, per-branch DBSCAN $\varepsilon$ (`tab:exp:hparams_attn`). Include $\varepsilon$ for Vanilla emitter-only / mode-only and for RoPE-TOA / Bias emitter-only. Rotary scales are $\gamma_t=\gamma_x=\gamma_y=10$ in the method chapter, not this table.
- `04_evaluation_protocol.tex` — Scoring protocol for the rest of the chapter. All models scored at $L=2000$ with no overlap on the same $1169$ test windows over $244$ scenarios, including Feature DBSCAN. Spread is over windows. Do not mention training seeds. Roadmap: RQ1 in `05_vanilla_vs_dbscan.tex`, RQ2 in `06_joint_objective.tex`, RQ3 in `07_attention_variants.tex` (joint bake-off, then emitter-only copies of RoPE-TOA and Bias in `tab:exp:attn_em_task`). Labels: `sec:exp:protocol` and `sec:exp:results:protocol`.
- `05_vanilla_vs_dbscan.tex` — RQ1 only. Joint Vanilla versus Feature DBSCAN, both branches in one table (`tab:exp:vanilla`). Do not introduce attention variants here. Labels: `sec:exp:results` and `sec:exp:results:vanilla`. Forward-ref the Vanilla panels of `hist_em.pdf` / `kcorr_em.pdf` in `07_attention_variants.tex`. Point at `fig:exp:feat_scan`, `fig:exp:feat_imbalance`, `fig:exp:feat_maxemitters`, and `fig:exp:feat_maxmode` for why Feature DBSCAN oversplits; do not re-include those figures. Global mode catalogue $M=19$.
- `06_joint_objective.tex` — RQ2 only, before the attention bake-off. Vanilla backbone; Joint vs Emitter-only vs Mode-only; score the active branch only (Joint scores both). Dual-branch architecture kept; inactive branch excluded from the backward pass and not scored. Joint numbers copied from `tab:exp:vanilla`, not a second run. Mode-only is part of the design (`final_vanilla_mode`). Two tables, no branch column: emitter (`tab:exp:ablation_em`) and mode (`tab:exp:ablation_md`). Count heatmaps `kcorr_ablation_{em,md}.pdf` (1×2: emitters Joint vs Emitter-only; modes Joint vs Mode-only), generated by `scripts/plot_eval_figures.py` from `agg_results_new/`. Joint panels are the Vanilla evaluation, not a second run. Do not add a joint-vs-separate UMAP here (it is in `app:embeddings`); do not add ECDFs. Do not add RoPE-TOA or Bias single-task tables. Do not compare ablation counts to RoPE-TOA or Bias; that comparison belongs in `07_attention_variants.tex`. Do not report unused-head scores. Label: `sec:exp:ablation`.
- `07_attention_variants.tex` — RQ3. Joint attention variants: Vanilla, RoPE-TOA, RoPE-az, Bias, and RoPE-TOA+Bias (`final_combined`: TOA RoPE on trunk and both branches, plus the pairwise bias, no incidence RoPE; same train overlap $1800$ as the other neural runs). Include all five in the score and efficiency tables. Do not repeat Feature DBSCAN in the score tables; it stays in `tab:exp:vanilla` and in the efficiency table (`final_baseline`: no training, test $L=2000$, overlap $0$, same $1169$ windows). Vanilla remains the baseline row. Tables: emitter (`tab:exp:attn_em`), mode (`tab:exp:attn_md`), parameters and encoder/clustering time (`tab:exp:efficiency`). After the joint bake-off, a compact emitter table (`tab:exp:attn_em_task`, `sec:exp:attn_task`) of Vanilla / RoPE-TOA / Bias under joint vs $\Loss_{\mathrm{em}}$ only (`final_vanilla_deint`, `final_pure_rope_deint`, `final_physical_bias_deint`). Joint and Vanilla emitter-only rows are copied, not a second evaluation. RoPE-TOA emitter-only matches joint; Bias emitter-only does not (ARI $0.840$). No mode-only RoPE/Bias rows; no emitter-only RoPE-az or RoPE-TOA+Bias. Overlaid ARI ECDFs on a log vertical axis (`hist_{em,md}.pdf`) and count heatmaps (`kcorr_{em,md}.pdf`, 3×2 with one empty cell), generated by `scripts/plot_eval_figures.py` from `agg_results_new/` (no stride-10 subsample). Do not put Feature DBSCAN in those figures. Count-heatmap reading matches `kcorr_ablation_{em,md}.pdf` in `06_joint_objective.tex`. A factual link to the objective comparison is in scope: Emitter-only Vanilla already reaches the closed emitter range; RoPE-TOA and Bias reach it under the joint objective; RoPE-TOA+Bias does not. Point at `app:ari_all` for the two-column ARI tables and the four-curve CDFs (Vanilla, Vanilla single-task, RoPE-TOA, Bias). Learned bias coefficients (`tab:exp:bias_coef`, `sec:exp:results:bias_coef`) are the median and quartiles over the 32 scalars of each stack, not the mean.
- Do not restore `08_qualitative_analysis.tex`. Feature-over-ToA plots belong in `02_datasets.tex`. A qualitative embedding gallery is only worth adding if it shows a failure window that the tables do not already cover.

## Style notes

- Tables: use `booktabs` (`\toprule`, `\midrule`, `\bottomrule`) and `siunitx` `S` columns for numerical alignment once numbers exist. Bold winners with `\textbf{...}`.
- Numbers: report 3 significant figures. Table spread is over windows. Do not mention training seeds.
- Figures live in `figures/experiments/`.
- Never report a number without saying which metric, which dataset, and how it was aggregated.
- Avoid claims like "significantly better" without a stated test. "Higher by X std" is acceptable.
